<resources>

    <!-- Spacing -->
    <dimen name="nav_header_vertical_spacing">16dp</dimen>

    <!-- Paddings -->
    <dimen name="no_padding">0dp</dimen>
    <dimen name="default_padding">8dp</dimen>
    <dimen name="slight_padding">4dp</dimen>

    <!-- Margins -->
    <dimen name="no_margin">0dp</dimen>
    <dimen name="quarter_margin">4dp</dimen>
    <dimen name="half_margin">8dp</dimen>
    <dimen name="full_margin">16dp</dimen>

    <!-- Elevations -->
    <dimen name="default_elevation">4dp</dimen>

    <!-- Text Sizes -->
    <dimen name="extra_extra_large_text_size">20sp</dimen>
    <dimen name="extra_large_text_size">14sp</dimen>
    <dimen name="large_text_size">12sp</dimen>
    <dimen name="normal_text_size">10sp</dimen>
    <dimen name="small_text_size">8dp</dimen>
    <dimen name="extra_small_text_size">6sp</dimen>

    <!-- Width -->
    <dimen name="min_widget_width">250dp</dimen>

    <!-- Height -->
    <dimen name="map_height">160dp</dimen>
    <dimen name="nav_header_height">160dp</dimen>
    <dimen name="min_widget_height">250dp</dimen>

    <!-- *** -->

    <!-- Search Result -->
    <dimen name="search_result.label.text_size">@dimen/small_text_size</dimen>
    <dimen name="search_result.value.resident.text_size">@dimen/small_text_size</dimen>
    <dimen name="search_result.value.non_resident.text_size">@dimen/extra_small_text_size</dimen>

    <!-- Settings -->
    <dimen name="settings.text_size">@dimen/normal_text_size</dimen>

</resources>
