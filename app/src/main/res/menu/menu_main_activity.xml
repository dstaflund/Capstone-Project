<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.activity.HomeActivity">

    <item android:id="@+id/search"
          android:title="@string/search_hint"
          android:icon="@drawable/ic_menu_search"
          app:showAsAction="collapseActionView|withText|ifRoom"
          app:actionViewClass="android.support.v7.widget.SearchView" />

    <group
        android:id="@+id/menu_item_home_layer_group"
        android:checkableBehavior="single"
        android:title="Layer">
        <item
            android:id="@+id/menu_home_activity_layer_normal"
            android:icon="@drawable/ic_crop_original_black_24dp"
            android:checked="true"
            android:title="Normal"/>
        <item
            android:id="@+id/menu_home_activity_layer_terrain"
            android:icon="@drawable/ic_terrain_black_24dp"
            android:title="Terrain" />
        <item
            android:id="@+id/menu_home_activity_layer_satellite"
            android:icon="@drawable/ic_satellite_black_24dp"
            android:title="Satellite" />
        <item
            android:id="@+id/menu_home_activity_layer_hybrid"
            android:icon="@drawable/ic_menu_gallery"
            android:title="Hybrid" />
    </group>
    <item
        android:id="@+id/menu_home_clear_search_history"
        android:orderInCategory="90"
        android:title="Clear Search History"
        app:showAsAction="never" />
</menu>
